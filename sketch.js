// var x = 10;

function setup() {
    createCanvas (600, 600); 
    noCursor(); 
    
} 

function draw() { 
    background(100); 
    fill(255, 178, 202);
    square(mouseX, mouseY, 55, 20); 
    
} 
